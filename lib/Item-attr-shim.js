// Generated by CoffeeScript 1.4.0

module.exports = function(Item) {
  var div, propertyNameFix;
  div = document.createElement('div');
  div.setAttribute('class', 't');
  if (div.className !== 't') {
    Item.prototype.getAttr = function(name) {
      var attr, el;
      el = this.el;
      name = propertyNameFix[name] || name;
      if (el.getAttribute) {
        attr = el.getAttribute(name);
      } else {
        attr = this.getProp(name);
      }
      if (attr != null) {
        if (attr === true) {
          attr = name;
        } else {
          attr += '';
        }
      } else {
        attr = null;
      }
      return attr;
    };
    Item.prototype.setAttr = function(name, val) {
      var el;
      el = this.el;
      name = propertyNameFix[name] || name;
      if (el.setAttribute) {
        el.setAttribute(name, val);
      } else {
        this.setProp(name, val);
      }
    };
  }
  propertyNameFix = {
    tabindex: 'tabIndex',
    readonly: 'readOnly',
    'for': 'htmlFor',
    'class': 'className',
    maxlength: 'maxLength',
    cellspacing: 'cellSpacing',
    cellpadding: 'cellPadding',
    rowspan: 'rowSpan',
    colspan: 'colSpan',
    usemap: 'useMap',
    frameborder: 'frameBorder',
    contenteditable: 'contentEditable'
  };
};
